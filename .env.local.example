# Local Postgres (use Docker, see README)
DATABASE_URL="postgresql://postgres:saaf@localhost:5432/saaf"

# NextAuth — local dev
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="generate-with-openssl-rand-base64-32"

# GitHub OAuth App (Settings → Developer settings → OAuth Apps in SAAF-Project org)
# Callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_ID="Ov23li..."
GITHUB_SECRET="..."

# Server-side PAT (fine-grained, all repos in SAAF-Project org)
# Permissions needed:
#   Repository: Contents:Read, Pull requests:Read, Issues:Read
#   Organization: Members:Read
GITHUB_PAT="github_pat_..."
